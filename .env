# .env only use in node.js
HOSTNAME=localhost
PORT=4000
HOST=http://$HOSTNAME:$PORT
# expose a variable to the browser with `process.env.NEXT_PUBLIC_ANALYTICS_ID`
NEXT_PUBLIC_ANALYTICS_ID=abcdefghijk
testsiteUrl=https://localhost:3000

siteUrl=https://side-project-frontend-iota.vercel.app
backendUrl=https://nodal-buckeye-404908.de.r.appspot.com/api/
```